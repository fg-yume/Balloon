"use strict";

// Encompass app within this variable
var app = app || {};

// Handles the controls for the game
app.input = {
	mouse : new app.Mouse(0,0),
	keydown : [],

	// functions -------------------------------
	
	update : function()
	{
		
	},
};